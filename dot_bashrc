#
# ~/.bashrc
#

# Set shell-specific prompt character BEFORE Dorothy loads
export SHELL_PROMPT_CHAR='b>'

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

source '/home/mcrowe/.local/share/dorothy/init.sh' # Dorothy
. "$HOME/.cargo/env"